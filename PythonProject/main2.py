import pandas as pd

df = pd.read_excel("data.xlsx")

# fullage = df[df["age"] > 50]

# total_age = df["age"].sum()
# mean_age = df["age"].mean()

# ages = df["age"].tolist()
# with open("ages.txt", "w", encoding="utf-8") as f:
#     for age in ages:
#         f.write(str(age) + " ")